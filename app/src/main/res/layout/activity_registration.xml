<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".ui.registration.RegistrationActivity">
    <LinearLayout
        android:id="@+id/registration_fragment_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/black"
        android:layout_above="@+id/header"
        android:orientation="horizontal">
    <ImageView
        android:id="@+id/back"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:layout_marginTop="10dp"
        android:src="@drawable/ic_arrow_back_white_24dp"/>
        <com.beanstack.utility.TextWithIconHeaderView
            android:id="@+id/fragmentTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:headerTitle="@string/fragment_title">
        </com.beanstack.utility.TextWithIconHeaderView>
    </LinearLayout>
    <LinearLayout
        android:id="@+id/registration_fragment_progress"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/registration_fragment_header"
        android:orientation="vertical">
    <include layout="@layout/stepprogressbar" />
    </LinearLayout>
    <FrameLayout
        android:id="@+id/registration_fragment_layout"
        android:layout_below="@+id/registration_fragment_progress"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">
    <androidx.viewpager.widget.ViewPager
        android:id="@+id/pager"
        android:layout_below="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
    </FrameLayout>

    <include layout="@layout/footer_buttons_layout" />
</RelativeLayout>